┌─────────────────────────────────────────────────────────────────────────────────┐
│                              O R D I V O N                                    │
│                    Governance Operating System Architecture                    │
│                                                                               │
│  443 documents · 38 checkers · 16 CI jobs · 12 lessons · 5 debts · 4 CRs     │
└─────────────────────────────────────────────────────────────────────────────────┘

┌── PHILOSOPHY & METHODOLOGY (L0) ───────────────────────────────────────────────┐
│                                                                               │
│  ┌─────────────────────┐  ┌──────────────────────┐  ┌──────────────────────┐ │
│  │ 12 L0 Invariants     │  │ Dual-Gate Loop       │  │ A1-A4 Disposition    │ │
│  │                      │  │                      │  │                      │ │
│  │ Evidence ≠ Claim     │  │ Intent → Scope       │  │ A1: Direct Fix       │ │
│  │ READY ≠ Authorization│  │   ↓                  │  │ A2: Logic Refine     │ │
│  │ Checker ≠ Policy     │  │ Pre-Execution Gate   │  │ A3: System Redesign  │ │
│  │ Generated ≠ Source   │  │   ↓                  │  │ A4: Debt Formalize   │ │
│  │ Ignore ≠ Resolution  │  │ Execution → Receipt  │  │                      │ │
│  │ Suppress ≠ Fix       │  │   ↓                  │  │ Controlled Vocabulary│ │
│  │ Derived ≠ Duplicated │  │ Post-Execution Gate  │  │ Assessment Template  │ │
│  │ Analogy ≠ Proof      │  │   ↓                  │  │ AI Observer Role     │ │
│  │ Summary ≠ Truth      │  │ PolicyCandidate      │  │ Not Clean. Governed. │ │
│  └─────────────────────┘  └──────────────────────┘  └──────────────────────┘ │
└───────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        │ defines & constrains
                                        ▼
┌── CORE — GOVERNANCE KERNEL ───────────────────────────────────────────────────┐
│                                                                               │
│  State Algebra:  READY / BLOCKED / DEGRADED                                   │
│  Closure:        CandidateClosure → VerifiedClosure                           │
│  Authority:      source_of_truth > current_status > supporting_evidence       │
│                                                                               │
│  ┌──────────────────────────────────────────────────────────────────────┐    │
│  │                        CHECKER ECOSYSTEM (38)                        │    │
│  │                                                                      │    │
│  │  Moat (6)        Document (7)     Coding (6)     PGI (4)            │    │
│  │  ─────────       ────────────     ──────────     ──────             │    │
│  │  agentic-pat     doc-registry     coding-smoke   pgi-confidence     │    │
│  │  arch-bound      doc-freshness    coding-fixt    pgi-decision       │    │
│  │  protected-p     current-truth    coverage-gov   pgi-evidence       │    │
│  │  agent-native    receipt-integ    dependabot     pgi-failure        │    │
│  │  philosophy      dogfood-evid     finance-bnd                        │    │
│  │  ownership       runtime-evid     trading-disc    Protocol (3)      │    │
│  │                                    ───────────                      │    │
│  │  Operational (12)                  ogap-payload                     │    │
│  │  ────────────────                  hap-payload                      │    │
│  │  entropy-telem, entropy-gate, checker-maturity, owner-activation    │    │
│  │  gate-manifest, policy-shadow, lesson-extraction, state-truth       │    │
│  │  external-src, verification-debt, egb3, coverage-gov, manifest      │    │
│  └──────────────────────────────────────────────────────────────────────┘    │
│                                                                               │
│  ┌──────────────────────────┐  ┌──────────────────────────────────────────┐  │
│  │    CI GATES (16 jobs)    │  │        SCHEMA SYSTEM (5 schemas)         │  │
│  │                          │  │                                          │  │
│  │  Blocking (14):          │  │  document-types.json                     │  │
│  │   ruff, pr-fast          │  │  governed-exclusions.json                │  │
│  │   governance-self-check  │  │  protected-paths-config.json             │  │
│  │   atomic-governance      │  │  agentic-patterns-config.json            │  │
│  │   verify-native          │  │  claim-vocabulary.json                   │  │
│  │   product/governance/    │  │                                          │  │
│  │   finance/coding tests   │  │  Generated Views:                        │  │
│  │   frontend, evals        │  │   update-registry-stats.py               │  │
│  │   verify-external        │  │   _registry-stats.md (generated_view)    │  │
│  │                          │  │                                          │  │
│  │  Advisory (2):           │  │  Atomic Gate:                            │  │
│  │   entropy-telemetry      │  │   check_atomic_governance.py             │  │
│  │   policy-shadow          │  │   308 files, 4 dirs                      │  │
│  │                          │  │                                          │  │
│  │  Security (3):           │  │  Authority Gate:                         │  │
│  │   pip-audit, pnpm audit  │  │   check_authority_gate.py                │  │
│  │   Gitleaks, CodeQL       │  │   442 docs, 0 no-owner                   │  │
│  └──────────────────────────┘  └──────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────────────────────────────┘
          │                              │                              │
          │ constrains                   │ governs                      │ applies to
          ▼                              ▼                              ▼
┌── PACKS ───────────────┐  ┌── DOCUMENT UNIVERSE ──┐  ┌── ADAPTERS ────────────┐
│                        │  │                        │  │                        │
│  coding/               │  │  443 entries           │  │  finance/              │
│   CodingDiscipline     │  │  22 doc_types          │  │   AlpacaObservation    │
│   Policy               │  │  6 layers (L0-L5)      │  │   Provider             │
│                        │  │                        │  │   (paper, GET-only)     │
│  finance/              │  │  Registry + Schema     │  │   4 security guards    │
│   TradingDiscipline    │  │  Dual Checker Sync     │  │                        │
│   Policy               │  │  Atomic Governance     │  │  runtimes/             │
│   Decision Intake      │  │  Overclaim Detection   │  │   Hermes runtime       │
│                        │  │                        │  │   adapter              │
└────────────────────────┘  └────────────────────────┘  └────────────────────────┘
                                                              │
                                                              │ touches
                                                              ▼
┌── KNOWLEDGE ASSETS ───────────────────────────────────────────────────────────┐
│                                                                               │
│  ┌────────────────────┐  ┌────────────────────┐  ┌──────────────────────────┐│
│  │ Lesson Ledger (12) │  │ Debt Ledger (7)     │  │ CandidateRules (4)       ││
│  │                    │  │                    │  │                          ││
│  │ 8 actioned         │  │ 3 CLOSED           │  │ CR-bcf8bb (freshness)    ││
│  │ 4 pending review   │  │ 4 OPEN:            │  │ CR-32b58 (overclaim)     ││
│  │                    │  │  · CVE-2026-3219   │  │ CR-ci-selcal-001 (drift) ││
│  │ SHA256 per-entry   │  │  · CI product-tests│  │ CR-ci-selcal-002 (dual)  ││
│  │ Review pipeline    │  │  · Lesson review   │  │                          ││
│  │ active (8/12)      │  │  · Draft checkers  │  │ All draft. 0 activated.  ││
│  └────────────────────┘  └────────────────────┘  └──────────────────────────┘│
└───────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        │ feeds back into
                                        ▼
┌── SELF-CALIBRATION LOOP ─────────────────────────────────────────────────────┐
│                                                                               │
│   Reality drift → CI BLOCKED → Freeze → Classify → A1/A2/A3/A4 → Execute     │
│       ↑                                                              │       │
│       └──────── Lesson → CandidateRule → Methodology ←──────────────┘       │
└───────────────────────────────────────────────────────────────────────────────┘

┌── EXTERNAL BOUNDARIES ────────────────────────────────────────────────────────┐
│                                                                               │
│  ┌─────────────────────────────┐    ┌────────────────────────────────────┐   │
│  │ Human Developer             │    │ AI Collaborator                    │   │
│  │                             │    │                                    │   │
│  │ git push → CI gate          │    │ AI Observer:                       │   │
│  │ Blocked by:                 │    │   may propose, classify, draft     │   │
│  │  ruff, tests, verify-native │    │   may NOT authorize, close,        │   │
│  │  atomic-governance,         │    │   suppress, or resolve debt        │   │
│  │  governance-self-check      │    │                                    │   │
│  │                             │    │ Governed by:                       │   │
│  │ Pull request → review gate  │    │  agent-output-contract.md          │   │
│  └─────────────────────────────┘    │  detect_overclaim.py               │   │
│                                      │  agentic-patterns checker          │   │
│  ┌─────────────────────────────┐    └────────────────────────────────────┘   │
│  │ External Systems            │                                              │
│  │                             │    ┌────────────────────────────────────┐   │
│  │ Alpaca Markets (paper)      │    │ Governance Boundary                │   │
│  │ GitHub (CI, PR, issues)     │    │                                    │   │
│  │ PyPI (dependency audit)     │    │ AI without adapter:                │   │
│  │ OSV (CVE database)          │    │  ✗ Not constrained to vocabulary   │   │
│  │ Dependabot (auto-updates)   │    │  ✗ Can use synonyms to bypass      │   │
│  └─────────────────────────────┘    │  ✗ No structured output enforced   │   │
│                                      └────────────────────────────────────┘   │
└───────────────────────────────────────────────────────────────────────────────┘
