{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ordivon.dev/schemas/ordivon.verify.schema.json",
  "title": "Ordivon Verify External Configuration",
  "description": "Minimal external configuration schema for Ordivon Verify. Describes an external repo's verification setup without requiring full Ordivon DG Pack fields. This is a prototype artifact — not a public release contract.",
  "type": "object",
  "required": ["schema_version", "project_name", "mode"],
  "properties": {
    "schema_version": {
      "type": "string",
      "description": "Schema version identifier.",
      "examples": ["0.1"]
    },
    "project_name": {
      "type": "string",
      "description": "Human-readable project name for report display."
    },
    "mode": {
      "type": "string",
      "enum": ["advisory", "standard", "strict"],
      "description": "Verification mode. advisory: warnings allowed. standard: configured checks must pass. strict: all checks required."
    },
    "pack": {
      "type": "string",
      "enum": ["coding"],
      "description": "Governed-work pack. v1 supports the coding pack only."
    },
    "profile": {
      "type": "string",
      "enum": ["coding", "ai_coding_trust_audit"],
      "description": "Trust profile. v1 maps coding to ai_coding_trust_audit."
    },
    "risk_stage": {
      "type": "string",
      "enum": ["vibe", "merge", "release"],
      "description": "Coding trust stage for evidence expectations and reporting."
    },
    "receipt_paths": {
      "type": "array",
      "description": "Relative paths to Markdown receipt/claim files or directories containing receipt markdown files.",
      "items": { "type": "string" }
    },
    "debt_ledger": {
      "type": "string",
      "description": "Optional path to verification-debt-ledger.jsonl."
    },
    "gate_manifest": {
      "type": "string",
      "description": "Optional path to verification-gate-manifest.json."
    },
    "document_registry": {
      "type": "string",
      "description": "Optional path to document-registry.jsonl."
    },
    "output": {
      "type": "string",
      "enum": ["human", "json"],
      "default": "human",
      "description": "Output format."
    }
  },
  "additionalProperties": false
}
