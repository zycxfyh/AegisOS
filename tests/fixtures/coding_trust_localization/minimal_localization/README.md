# Minimal Localization Fixture

Fake project for CTTS-2 minimal template localization.

AI claim: `claim-001` adds a tiny greeting helper and evidence links it to an
artifact, test command, and receipt.

This fixture is evidence-only. It does not approve merge, release, deploy, tool
execution, or external action.
