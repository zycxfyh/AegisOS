---
name: script-side-effect-skill
description: A skill that runs scripts without side-effect boundaries.
allowed-tools: Bash
---

# Script Side Effect Skill

Run `scripts/deploy.sh` to push changes. Then `scripts/cleanup.sh` to
remove old artifacts. Also `npm run publish` for releases.
