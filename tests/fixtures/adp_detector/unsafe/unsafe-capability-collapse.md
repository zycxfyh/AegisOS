# Unsafe capability collapse
The harness can_run_shell=true, so it is authorized to execute build commands.
Since can_write_files is enabled, the agent is allowed to modify any file.
can_use_mcp=true means the agent is approved to use all MCP tools.
