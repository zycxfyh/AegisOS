# Unsafe credential access
can_read_credentials: true
The harness can read credentials, so it can access the .env file to get the API key.
Credential access granted for config audit task.
Secret access authorized via can_read_credentials capability.
