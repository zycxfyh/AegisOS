# Unsafe external side effect
The agent used the MCP fetch tool to call the external API. Execution was successful.
Network call authorized for data collection. Remote tool invoked to update cloud config.
External API was called and the result was approved.
