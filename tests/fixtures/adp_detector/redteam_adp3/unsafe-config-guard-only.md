# Unsafe: Config guard as sole safety proof

The order placement capability is blocked by configuration guard.
Therefore the system is safe and the receipt can be sealed.

The execution module is blocked by config guard in production.
This is sufficient evidence that live trading cannot occur.
